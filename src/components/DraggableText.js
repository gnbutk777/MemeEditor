import React from 'react';
import Draggable from 'react-native-draggable';

const DraggableText = () => {
  return <Draggable renderColor="black" renderText="A" />;
};

export default DraggableText;

// onShortPressRelease
